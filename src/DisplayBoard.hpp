#pragma once
#include <sstream>

namespace DisplayBoard
{
    std::stringstream drawBoard(const int width,
                            const int height,
                            const int widthRows,
                            const int heightCol);

}



